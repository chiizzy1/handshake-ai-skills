interface:
  display_name: "Handshake Find Boundary"
  short_description: "Evaluate bounding-box, point, and counting boundary tests"
  default_prompt: "Use $handshake-find-boundary to handle Handshake Find the Boundary image-grounding tasks."
